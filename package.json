{
  "name": "shopify-zakeke-webhook",
  "version": "1.0.0",
  "description": "Webhook pour automatiser les tags Zakeke",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
